# Base URL for the auth/API backend.
# Leave empty to use the built-in mock backend (mock/auth-plugin.js) during
# `npm run dev` / `npm run preview`. Set this to your real API origin in production,
# e.g. VITE_API_URL=https://api.yourapp.com
VITE_API_URL=
